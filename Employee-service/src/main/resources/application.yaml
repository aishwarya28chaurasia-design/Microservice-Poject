spring:
  application:
    name: Employee-service

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/employeeservice
    username: root
    password: root

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        jdbc:
          batch_size: 50 #  30-50 is usually the "sweet spot"
          order_inserts: true
          order_updates: true
          generate_statistics: true

springdoc:
  swagger-ui:
    path: /api-docs

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
    instance:
      hostname: localhost

#address:
#  service:
#    url: http://localhost:8082/addresses

server:
  port: 8081